# MCP C# SDK Документация

Сервер на основе **Model Context Protocol (MCP)** для индексирования и поиска документации C# SDK с использованием векторного поиска и переранжирования документов.

## Возможности

✨ **Основные функции:**
- 🔗 Автоматическая синхронизация документации из Git репозитория
- 🔍 Векторный поиск с использованием embedding'ов
- 📊 Переранжирование результатов поиска (BM25 и нейросетевое)
- 📚 Поддержка разных стратегий разбиения текста (Markdown, API/SDK)
- 💾 Постоянное хранилище индексов в ChromaDB
- 🌐 HTTP API для интеграции

## Требования

- Python >= 3.13
- Git (для синхронизации репозитория)
- AI сервер (например, Ollama или LM Server) с поддержкой:
  - Embedding моделей
  - LLM моделей (для переранжирования)

## Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd mcp-csharp-docs
```

### 2. Установка зависимостей

```bash
pip install -e .
```

Или через `uv` (рекомендуется):

```bash
uv sync
```

### 3. Конфигурация

Создайте файл `.env` в корне проекта с переменными окружения:

```env
GIT_REPO_URL=https://github.com/your-org/csharp-sdk-docs.git
DOCS_PATH=./docs
AI_SERVER_URL=http://localhost:11434
EMBEDDING_MODEL=nomic-embed-text
RERANK_MODEL=neural-rerank-model
CHUNK_SIZE=1024
CHUNK_OVERLAP=128
CHROMA_PERSIST_DIR=./chroma_db
COLLECTION_NAME=csharp_docs
REGISTRY_COLLECTION_NAME=docs_registry
```

**Описание переменных:**
| Переменная | Описание |
|-----------|---------|
| `GIT_REPO_URL` | URL репозитория с документацией (HTTPS или SSH) |
| `DOCS_PATH` | Локальный путь для хранения документации |
| `AI_SERVER_URL` | URL AI сервера (Ollama, LM Server и т.д.) |
| `EMBEDDING_MODEL` | Модель для создания embedding'ов |
| `RERANK_MODEL` | Модель для переранжирования документов |
| `CHUNK_SIZE` | Размер чанка при разбиении текста (символы) |
| `CHUNK_OVERLAP` | Перекрытие между чанками (символы) |
| `CHROMA_PERSIST_DIR` | Директория для хранения ChromaDB |
| `COLLECTION_NAME` | Имя основной коллекции в ChromaDB |
| `REGISTRY_COLLECTION_NAME` | Имя коллекции реестра файлов |

## Использование

### Запуск сервера

```bash
python server8.py
```

Сервер запустится на `http://127.0.0.1:8000` с MCP endpoint на `/mcp`

### Доступные инструменты MCP

#### 1. `search_sdk_docs`
Поиск в документации C# SDK по заданному запросу.

**Параметры:**
- `query` (str): Поисковый запрос

**Возвращает:** Список релевантных документов с метаданными

**Пример:**
```python
result = await search_sdk_docs("как использовать HTTP клиент")
```

#### 2. `open_document`
Открыть конкретный markdown файл документации.

**Параметры:**
- `path` (str): Относительный путь к файлу в документации

**Возвращает:** Содержимое файла

**Пример:**
```python
content = await open_document("getting-started/installation.md")
```

#### 3. `sync_docs_index`
Запустить индексацию документации (синхронизация с Git и обновление индексов).

**Возвращает:** Статус выполнения

**Пример:**
```python
status = await sync_docs_index()
```

#### 4. `fetch_url_text`
Загрузить текст со страницы по URL.

**Параметры:**
- `url` (str): URL страницы

**Возвращает:** Текстовое содержимое страницы

#### 5. `fetch_page_links`
Получить все ссылки со страницы.

**Параметры:**
- `url` (str): URL страницы

**Возвращает:** Список ссылок

## Архитектура

```
server8.py (основной MCP сервер)
├── RecursiveMarkdownTextSplitter.py (разбиение Markdown)
├── ApiSdkSplitter.py (разбиение API/SDK документации)
├── RecursiveTextSplitter.py (базовое разбиение текста)
├── bm25.py (переранжирование BM25)
├── clean_md.py (очистка Markdown)
├── config.py (загрузка конфигурации)
└── embeddings_example.py (примеры работы с embedding'ами)

Хранилище:
├── chroma_db/ (векторная база данных ChromaDB)
└── docs/ (локальная копия документации)
```

## Процесс работы

### Индексация документов

1. **Синхронизация с Git**: Клонирование или обновление репозитория документации
2. **Сканирование файлов**: Поиск всех `.md` файлов на диске
3. **Сравнение с БД**: Определение новых, модифицированных и удаленных файлов
4. **Разбиение текста**: Выбор оптимального сплиттера на основе содержимого
5. **Генерация embedding'ов**: Создание векторных представлений для каждого чанка
6. **Добавление в ChromaDB**: Сохранение чанков и их метаданных

### Поиск документов

1. **Создание embedding'а запроса**: Преобразование поискового запроса в вектор
2. **Векторный поиск**: Поиск похожих документов в ChromaDB (top-20)
3. **Переранжирование**: Сортировка результатов через BM25 или нейросеть
4. **Расширение контекста**: Загрузка связанных документов по ссылкам
5. **Возврат результатов**: Форматированный вывод (top-5)

## Компоненты

### RecursiveMarkdownTextSplitter
Умное разбиение Markdown-документов с сохранением структуры и связей.

### ApiSdkSplitter
Специализированное разбиение для API/SDK документации (таблицы, параметры и т.д.).

### BM25 Переранжирование
Классический алгоритм переранжирования на основе TF-IDF, быстрый и надежный.

### Векторный поиск
Использует embedding'и из локального AI сервера для поиска по смыслу.

## Логирование

Все операции логируются с уровнями:
- `INFO`: Основные операции (синхронизация, поиск)
- `DEBUG`: Детальная информация (обработка файлов, чанки)
- `ERROR`: Ошибки и исключения

Логи выводятся в консоль с timestamp'ами.

## Примеры

### Пример 1: Поиск в документации

```python
import asyncio
from server8 import handle_call_tool

async def main():
    result = await handle_call_tool("как создать HTTP клиент")
    print(result)

asyncio.run(main())
```

### Пример 2: Синхронизация документации

```python
import asyncio
from server8 import run_sync_task

async def main():
    result = await run_sync_task()
    print(result)

asyncio.run(main())
```

### Пример 3: Открытие конкретного документа

```python
import asyncio
from server8 import get_document

async def main():
    content = await get_document("api/http-client.md")
    print(content)

asyncio.run(main())
```

## Безопасность

- ✅ Проверка на обход директорий (path traversal prevention)
- ✅ Доступ только к файлам в `DOCS_PATH`
- ✅ Валидация путей при открытии файлов
- ✅ Защита от повторного запуска синхронизации

## Производительность

- Поиск в ChromaDB: < 1 сек для 20 кандидатов
- Переранжирование BM25: < 100 мс
- Генерация embedding'а: зависит от размера и модели
- Полная переиндексация: зависит от количества документов

## Стандартные модели

Рекомендуемые модели для локального запуска (Ollama):

```bash
# Embedding модель (быстрая, компактная)
ollama pull nomic-embed-text

# LLM модель (для переранжирования)
ollama pull mistral
# или
ollama pull neural-rerank-v1
```

## Решение проблем

### Ошибка подключения к AI серверу
Убедитесь, что `AI_SERVER_URL` верно указан и сервер запущен.

### Документация не синхронизируется
Проверьте права доступа на `DOCS_PATH` и корректность `GIT_REPO_URL`.

### Embedding'и не создаются
Убедитесь, что модель `EMBEDDING_MODEL` загружена на AI сервер.

### Результаты поиска неточные
Попробуйте увеличить `CHUNK_SIZE` или изменить модель embedding'а.

## Разработка

### Структура проекта

```
mcp-csharp-docs/
├── server8.py                           # Основной MCP сервер
├── config.py                            # Конфигурация
├── RecursiveMarkdownTextSplitter.py    # Markdown сплиттер
├── ApiSdkSplitter.py                   # API/SDK сплиттер
├── RecursiveTextSplitter.py            # Базовый сплиттер
├── bm25.py                             # BM25 переранжирование
├── clean_md.py                         # Очистка Markdown
├── embeddings_example.py               # Примеры
├── pyproject.toml                      # Конфиг проекта
├── .env                                # Конфигурация окружения
├── chroma_db/                          # БД с индексами
└── docs/                               # Документация SDK
```

### Добавление нового функционала

1. Добавьте функцию-обработчик
2. Оберните её в `@mcp.tool()` декоратор
3. Задайте имя и описание инструмента
4. Используйте `logging` для отладки

## Лицензия

MIT

## Автор

Проект был создан как инструмент для работы с документацией C# SDK.

## Поддержка

Для вопросов и проблем создавайте issues в репозитории.
